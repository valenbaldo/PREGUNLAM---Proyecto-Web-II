{{> header}}

<style>
    .res-wrap { max-width: 680px; margin: 48px auto; padding: 28px; border:1px solid #e5e7eb; border-radius:16px; text-align:center; box-shadow: 0 2px 10px rgba(0,0,0,.06);}
    .res-title { font-size: 26px; font-weight: 800; margin-bottom: 6px; }
    .res-score { font-size: 54px; font-weight: 900; margin: 8px 0 18px; }
    .btn { display:inline-block; margin-top:12px; padding:12px 18px; border:none; border-radius:12px; background:#111; color:#fff; font-weight:700; cursor:pointer; text-decoration:none; }
</style>

<div class="res-wrap">
    <div class="res-title">¡Partida finalizada! ✅</div>
    <div>Tu puntaje final es</div>
    <div class="res-score">{{puntaje}}</div>

    <a class="btn" href="?controller=juego&method=base">Jugar de nuevo</a>
    <a class="btn" style="margin-left:8px" href="?controller=home&method=base">Ir al inicio</a>
</div>

{{> footer}}
